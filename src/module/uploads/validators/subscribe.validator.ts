// import { FileTypeValidator, MaxFileSizeValidator, ParseFilePipe } from '@nestjs/common';

// import UploadsUtils from '../uploads.utils';

// export const subscribePipeValidator = new ParseFilePipe({
//   fileIsRequired: true,
//   validators: [
//     // new FileTypeValidator({ fileType: AllowedFileTypesEnum.pdfFile }),
//     new FileTypeValidator({fileType:'image/png'}),
//     // new FileTypeValidator({fileType:AllowedFileTypesEnum.docsFile}),
//     // new MaxFileSizeValidator({ maxSize: 100 * 1024 * 1024 }),
//   ],
//   exceptionFactory: UploadsUtils.getExceptionFactory('images'),
// });

// export default subscribePipeValidator;
